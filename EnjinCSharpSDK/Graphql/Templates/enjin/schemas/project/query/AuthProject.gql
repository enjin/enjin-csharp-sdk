#namespace enjin.sdk.project.AuthProject

#arg id Int!
#arg secret String!

query {
    result: AuthApp(
        id: $id,
        secret: $secret
    ) {
        accessToken
        expiresIn
    }
}
