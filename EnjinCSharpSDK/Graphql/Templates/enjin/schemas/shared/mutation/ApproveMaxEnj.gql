#namespace enjin.sdk.shared.ApproveMaxEnj
#import enjin.sdk.shared.Transaction

#arg ethAddress EthAddress
#arg send Boolean true

mutation {
    result: ApproveMaxEnj(
        wallet: $ethAddress,
        send: $send
    ) {
        ...Transaction
    }
}
