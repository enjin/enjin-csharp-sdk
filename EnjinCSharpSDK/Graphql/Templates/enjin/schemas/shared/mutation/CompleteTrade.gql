#namespace enjin.sdk.shared.CompleteTrade
#import enjin.sdk.shared.Transaction

#arg tradeId String!
#arg ethAddress EthAddress
#arg send Boolean true

mutation {
    result: CompleteTrade(
        tradeId: $tradeId,
        wallet: $ethAddress,
        send: $send
    ) {
        ...Transaction
    }
}
