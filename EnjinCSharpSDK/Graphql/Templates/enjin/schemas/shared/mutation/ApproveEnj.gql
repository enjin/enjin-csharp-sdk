#namespace enjin.sdk.shared.ApproveEnj
#import enjin.sdk.shared.Transaction

#arg value BigInt!
#arg ethAddress EthAddress
#arg send Boolean true

mutation {
    result: ApproveEnj(
        value: $value,
        wallet: $ethAddress,
        send: $send
    ) {
        ...Transaction
    }
}
